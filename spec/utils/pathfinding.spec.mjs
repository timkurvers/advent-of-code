import { astar, bfs } from '../../utils';

describe('pathfinding utilities', () => {
  describe('astar()', () => {
    it.todo('pathfinds');
  });

  describe('bfs()', () => {
    it.todo('pathfinds');
  });
});
